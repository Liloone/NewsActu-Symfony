<div class="row">

	<p class="d-flex justify-content-around ">

		{% for category in categories %}

			<span class="d-inline pt-2">
				<a href="{{ path('show_articles_from_category', {'alias': category.alias} ) }}" class="text-decoration-none text-light">{{ category.name }}</a>
			</span>

		{% endfor %}

	</p>

</div>

